[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m venv /opt/venv", ". /opt/venv/bin/activate", "cd /app && pip install -r backend/requirements.txt"]

[start]
cmd = ". /opt/venv/bin/activate && cd /app && python backend/cron_tasks.py fetch-market"
