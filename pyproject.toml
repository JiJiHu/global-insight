[tool.black]
line-length = 100
target-version = ['py311']
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
  | venv
  | archive
)/
'''

[tool.isort]
profile = "black"
line_length = 100
skip = ["venv", "archive", ".git"]
