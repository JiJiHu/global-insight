[build]
builder = "NIXPACKS"

[deploy]
startCommand = "./start.sh"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Cron 定时任务 - 通过环境变量触发 cron 模式
[[crons]]
name = "data-fetcher"
schedule = "0 */2 * * *"
command = "SERVICE_TYPE=cron ./start.sh"
