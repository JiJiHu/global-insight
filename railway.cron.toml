# Railway Cron 服务配置
[build]
builder = "NIXPACKS"

[deploy]
startCommand = ". /opt/venv/bin/activate && python backend/cron_tasks.py fetch-market"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
